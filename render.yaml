services:
  - type: web
    name: claimsure-api
    runtime: node
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
